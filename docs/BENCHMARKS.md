# Benchmarks

No benchmark numbers are checked into this repository yet.

Run locally:

```bash
cargo bench -p ironledger-benches
```

Record date, `rustc --version`, OS, CPU model, exact command, and configuration here after running on your machine. These are **local development measurements**, not production claims.
